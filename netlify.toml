[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# No SPA catch-all here: the build emits its own redirects so the server
# routes (including /api/public/scan) keep working on Netlify.
